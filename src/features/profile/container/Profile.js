import react from 'react';
import {View, Text} from 'native-base';

const Profile = () => {
    return (
        <View>
            <Text>
                This is the profile site, in construction.
            </Text>
        </View>
    );
};

export default Profile;
